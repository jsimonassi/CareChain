import React from 'react';
import {Text} from 'react-native';
import {Container} from './styles';

export const Login = () => {
  return (
    <Container>
      <Text>Login</Text>
    </Container>
  );
};
